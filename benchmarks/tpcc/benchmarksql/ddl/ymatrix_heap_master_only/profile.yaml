ddl_profile:
  id: ymatrix_heap_master_only
  benchmark: tpcc
  database_type: ymatrix
  storage_engine: heap
  test_mode: master_only
  status: approved
  strategy: heap_master_only
  notes:
    - tableCreates.sql uses USING heap and DISTRIBUTED MASTERONLY for all BenchmarkSQL TPC-C tables.
