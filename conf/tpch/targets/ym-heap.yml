# YMatrix heap master-only target fragment for TPC-H benchmark.
all:
  children:
    ymatrix_heap:
      vars:
        db_type: ymatrix
        storage_engine: heap
        test_mode: master_only
        tpch_ddl_profile: ym-heap

        db_host: 172.16.100.62
        db_port: 5433
        db_name: tpch
        db_user: zhangchen
        db_password: "YMatrix@123"

        config_user: mxadmin
        config_password: "mxadmin"
        config_workdir: /home/mxadmin/automan
        gpconfig_command: gpconfig

        database_parameters:
          max_connections: "1050"

        manual_parameter_commands: []
